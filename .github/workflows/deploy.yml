name: Execute Command on EC2 via SSM

on:
  push:
    branches:
      - main
  workflow_dispatch: # Allows manual trigger

jobs:
  run-on-ec2:
    runs-on: ubuntu-latest # GitHub-hosted runner

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Install AWS CLI (If Not Installed)
        run: |
          sudo apt-get update
          sudo apt-get install -y unzip
          if ! command -v aws &> /dev/null; then
            echo "AWS CLI not found. Installing..."
            curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
            unzip -o awscliv2.zip
            sudo ./aws/install
          fi
          aws --version
        shell: bash

      - name: Configure AWS Credentials
        run: |
          mkdir -p ~/.aws
          cp scripts/aws-credentials.txt ~/.aws/credentials
          chmod 600 ~/.aws/credentials
        shell: bash

      - name: Upload command.sh to S3
        run: |
          echo "Uploading command.sh to S3..."
          aws s3 cp scripts/command.sh s3://os-comparison-bucket/command.sh
        shell: bash

      - name: Execute Command on EC2 Instances via SSM and Upload Outputs
        run: |
          # Disable immediate exit to allow the loop to continue even if one iteration fails
          set +e

          while IFS= read -r server || [[ -n "$server" ]]; do
            echo "-------------------------------------"
            echo "Processing server with private IP: $server"
            
            # Retrieve the Instance ID for this private IP
            INSTANCE_ID=$(aws ec2 describe-instances \
              --filters "Name=private-ip-address,Values=$server" \
              --query "Reservations[*].Instances[*].InstanceId" --output text)
              
            if [[ -z "$INSTANCE_ID" ]]; then
              echo "No instance found for IP: $server"
              continue
            fi
            echo "Found Instance ID: $INSTANCE_ID for IP: $server"
            
            # First SSM command: download and execute command.sh from S3
            FIRST_COMMAND_ID=$(aws ssm send-command \
              --document-name "AWS-RunShellScript" \
              --targets "Key=instanceIds,Values=$INSTANCE_ID" \
              --parameters '{"commands":["aws s3 cp s3://os-comparison-bucket/command.sh /tmp/command.sh && chmod +x /tmp/command.sh && sudo bash /tmp/command.sh"]}' \
              --region us-east-1 \
              --query "Command.CommandId" --output text)
              
            if [[ -z "$FIRST_COMMAND_ID" ]]; then
              echo "Failed to send first SSM command to instance $INSTANCE_ID"
              continue
            fi
            
            echo "First command sent with Command ID: $FIRST_COMMAND_ID. Waiting for execution..."
            sleep 15  # Wait for command execution
            
            echo "Fetching first SSM command output for instance $INSTANCE_ID..."
            FIRST_SSM_OUTPUT=$(aws ssm get-command-invocation \
              --command-id "$FIRST_COMMAND_ID" \
              --instance-id "$INSTANCE_ID" \
              --region us-east-1 \
              --query '{Status:Status, StandardOutputContent:StandardOutputContent, StandardErrorContent:StandardErrorContent}' \
              --output json)
              
            FIRST_OUTPUT_FILE="ssm_first_output_${server}_${INSTANCE_ID}.json"
            echo "$FIRST_SSM_OUTPUT" > "$FIRST_OUTPUT_FILE"
            echo "First SSM output for instance $INSTANCE_ID saved to $FIRST_OUTPUT_FILE"
            
            echo "Uploading $FIRST_OUTPUT_FILE to S3..."
            aws s3 cp "$FIRST_OUTPUT_FILE" s3://os-comparison-bucket/ || echo "Failed to upload $FIRST_OUTPUT_FILE"
            
            # Second SSM command: copy /tmp/output.txt from instance to S3
            echo "Copying /tmp/output.txt from instance $INSTANCE_ID to S3..."
            SECOND_COMMAND_ID=$(aws ssm send-command \
              --document-name "AWS-RunShellScript" \
              --targets "Key=instanceIds,Values=$INSTANCE_ID" \
              --parameters '{"commands":["aws s3 cp /tmp/output.txt s3://os-comparison-bucket/output_'${server//'./'}'_'${INSTANCE_ID}'_output.txt"]}' \
              --region us-east-1 \
              --query "Command.CommandId" --output text)
              
            if [[ -z "$SECOND_COMMAND_ID" ]]; then
              echo "Failed to send second SSM command to instance $INSTANCE_ID"
              continue
            fi
            
            echo "Second command sent with Command ID: $SECOND_COMMAND_ID. Waiting for file copy..."
            sleep 30  # Wait for file transfer
            
            echo "Fetching second SSM command output for instance $INSTANCE_ID..."
            SECOND_SSM_OUTPUT=$(aws ssm get-command-invocation \
              --command-id "$SECOND_COMMAND_ID" \
              --instance-id "$INSTANCE_ID" \
              --region us-east-1 \
              --query '{Status:Status, StandardOutputContent:StandardOutputContent, StandardErrorContent:StandardErrorContent}' \
              --output json)
              
            # SECOND_OUTPUT_FILE="ssm_second_output_${server}_${INSTANCE_ID}.json"
            # echo "$SECOND_SSM_OUTPUT" > "$SECOND_OUTPUT_FILE"
            # echo "Second SSM output for instance $INSTANCE_ID saved to $SECOND_OUTPUT_FILE"
            
            # echo "Uploading $SECOND_OUTPUT_FILE to S3..."
            # aws s3 cp "$SECOND_OUTPUT_FILE" s3://os-comparison-bucket/ || echo "Failed to upload $SECOND_OUTPUT_FILE"
            
          done < scripts/serverlist.txt
        shell: bash

      - name: Trigger AWS Lambda for Comparison
        run: |
          echo "Triggering AWS Lambda function for comparison..."
          aws lambda invoke --function-name comparefiles --payload '{}' response.json
        shell: bash
